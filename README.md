# Limitless Exchange Trading Bot
A bot for real-time market monitoring and trading on Limitless Exchange.

## Setup
1. Clone repo: `git clone ...`
2. Install dependencies: `pip install -r requirements.txt`
3. Set up `.env` 
4. Run: `python main.py`

## Strategy
